<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="capstonegroup2.dataapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DailyReviewGraph">
            <intent-filter>
                <action android:name="android.intent.action.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- TODO: can be removed from final instance - here for testing purposes -->
        <activity android:name=".Daily_Review_Launch_Test">
            <intent-filter>
                <action android:name="android.intent.action.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".accountCreation.AccountCreation"
            android:label="@string/title_activity_account_creation"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".PasswordRecovery"
            android:label="@string/pr_title"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Challenges.ChallengeGUI"
            android:label="@string/c_title"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".Account_Modification.Account_Modification"
            android:label="@string/title_activity_account__modification"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>